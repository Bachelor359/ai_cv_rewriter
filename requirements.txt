# Core
python-docx>=1.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
lxml>=5.0.0

# LLM providers — install only what you need, or install all
openai>=1.30.0
anthropic>=0.40.0
google-generativeai>=0.8.0

# Token counting (used by tiktoken internally, optional)
tiktoken>=0.7.0
